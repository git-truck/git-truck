export const feature_flags = {
  /**
   * Enable the new color scheme for last changed metric that uses .
   */
  newLastChangedColorScheme: true,
  /**
   * Enable last changed metric to be shown as a gradient legend instead of segments.
   */
  lastChangedAsGrad: true,
  /**
   * Enable file size metric to be shown as a gradient legend instead of segments.
   */
  fileSizeAsGrad: false,
  /**
   * Enable legend selection summary
   */
  show_legend_highlight: false,
  /**
   * Group all non-code filetype extension under "Other" category
   */
  group_noncode_filetypes_in_other: true
}
