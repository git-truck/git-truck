Migration plan

- [x] Define a `Metric` object type
- [-] Create a `Metric` object for all metrics
- [ ]. Start by implementing `name`, `icon` and `getTooltipContent`
- [ ] Start thinking of what else we need to move to the metric type. We want do define everything related to a metric in the metric file, so that we can easily add new metrics and get an overview of how a metric is implemented. This means we need to also include needed sql queries, etc.
